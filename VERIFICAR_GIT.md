# Verificar que Git está Instalado

Después de instalar Git, ejecuta este comando en la terminal:

```bash
git --version
```

Debe mostrar algo como: `git version 2.43.0` (o similar)

Si funciona, continúa con los siguientes pasos.

